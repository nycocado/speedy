racing_teleop_node:
  ros__parameters:
    axis_steering: 0
    axis_throttle: 4
    axis_brake: 5
    scale_linear: 1.0
    deadzone: 0.05
    # scale_steering is injected dynamically at launch time based on URDF bounds
